// Estos objetos solo existen en el navegador

// console.log(document);
// console.log(window);

// En cambio en NodeJS existe:

console.log(os.userInfo());

// Podemos ingresar al cmd y ejecutar node desde ahi
// Nos permite cargar, guardar, etc. con archivos de JS
// comando: node

// tambien podemos correr archivos usando directamente el cmd:
//    node app.js

// node es multiplataforma